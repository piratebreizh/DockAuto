#include "ViewCreationRobot.h"
#include <QtWidgets>


ViewCreationRobot::ViewCreationRobot(QWidget *parent) : QDialog(parent)
{
    /*structureRobot = new QGroupBox;
    cargaisonRobot = new QGroupBox;

    QVBoxLayout * Layout = new QVBoxLayout(parent);

    champLargeur = new QLineEdit;
    champLongueur = new QLineEdit;
    champVitesse = new QLineEdit;

    Layout->addWidget(new QLabel(tr("Longueur")),champLongueur);
    Layout->widget(new QLabel(tr("Largeur")),champLargeur);


    groupeBox->setLayout(mainLayout);

    mainLayout->addWidget(groupeBox);

    this->setLayout(mainLayout);*/
}
